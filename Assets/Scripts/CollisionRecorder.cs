using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollisionRecorder : MonoBehaviour
{
    public int collisions_cone;
    // Start is called before the first frame update
    void Start()
    {
        collisions_cone = 0;
    }
}
